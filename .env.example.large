# =============================================================================
# CONFIGURAÇÃO ESPECÍFICA PARA BANCOS GRANDES (200GB+)
# =============================================================================
# Este arquivo de exemplo é otimizado para:
# - Bancos de dados com mais de 200GB
# - Tabelas individuais entre 30GB e 80GB
# - Ambientes de produção com zero locks
# - Backup com chunks automáticos para tabelas grandes

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES DOS SERVIDORES DE BANCO DE DADOS
# -----------------------------------------------------------------------------

# Servidor de origem (onde estão os dados para backup)
SOURCE_HOST=127.0.0.1
SOURCE_PORT=53306

# Servidor de destino (onde os backups serão restaurados)
DEST_HOST=127.0.0.1
DEST_PORT=3306

# Credenciais de acesso aos bancos de dados
DB_USERNAME=seu_usuario
DB_PASSWORD=sua_senha

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES DE BACKUP PARA BANCOS GRANDES
# -----------------------------------------------------------------------------

# Databases a serem incluídos no backup (separados por vírgula)
DATABASES=seu_database

# Agendamento do backup - Para bancos grandes, executar com menos frequência
# Sugestão: 1x por dia durante madrugada (menos carga no servidor)
BACKUP_TIME="0 2 * * *"

# Retenção mais longa para bancos grandes (recuperação é mais demorada)
RETENTION_DAYS=14

# SEMPRE ativar compressão para bancos grandes (economiza 60-80% do espaço)
BACKUP_COMPRESSION=true

# Prefixo dos arquivos de backup
BACKUP_PREFIX=backup_large

# -----------------------------------------------------------------------------
# OPÇÕES MYSQL OTIMIZADAS PARA BANCOS GRANDES - ZERO LOCKS
# -----------------------------------------------------------------------------

# MYSQLDUMP com zero locks e otimizações para tabelas grandes
MYSQLDUMP_OPTIONS="--single-transaction --quick --lock-tables=false --no-tablespaces --skip-lock-tables --skip-add-locks --routines --triggers --default-character-set=utf8mb4 --max_allowed_packet=2G --net_buffer_length=32K --extended-insert --disable-keys"

# Cliente MySQL otimizado para conexões longas
MYSQL_CLIENT_OPTIONS="--max_allowed_packet=2G --net_buffer_length=32K --connect_timeout=120"

# Não executar backup na inicialização (muito pesado)
RUN_ON_START=false

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES DE SISTEMA PARA BANCOS GRANDES
# -----------------------------------------------------------------------------

# Timezone do sistema
TZ=America/Sao_Paulo

# SEMPRE usar logs detalhados para monitoramento
LOG_LEVEL=INFO

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES DE PERFORMANCE PARA BANCOS GRANDES
# -----------------------------------------------------------------------------

# Timeout muito alto para operações longas (6 horas)
DB_TIMEOUT=21600

# Muitas tentativas para maior robustez
MAX_RETRY_ATTEMPTS=7

# Intervalo maior entre tentativas (recuperação de falhas)
RETRY_INTERVAL=60

# SEMPRE ativar logs detalhados para bancos grandes
ENABLE_DEBUG_LOGS=true

# SEMPRE verificar integridade (crítico para backups grandes)
VERIFY_BACKUP_INTEGRITY=true

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES CRÍTICAS PARA CHUNKING DE TABELAS GRANDES
# -----------------------------------------------------------------------------
# ⚡ CONFIGURAÇÃO ESPECÍFICA PARA TABELAS DE 30-80GB

# SEMPRE ativar chunking para bancos grandes
ENABLE_AUTO_CHUNKING=true

# Detectar tabelas maiores que 500MB para chunking
# Para tabelas de 30-80GB, todas serão processadas em chunks
CHUNK_SIZE_THRESHOLD_MB=500

# Tamanho otimizado para tabelas de 30-80GB
# 50000 registros = bom equilíbrio entre velocidade e estabilidade
CHUNK_SIZE=50000

# Timeout generoso para cada chunk (45 minutos)
# Tabelas grandes podem ter chunks que demoram muito
CHUNK_TIMEOUT=2700

# Muitas tentativas para chunks (ambiente produção)
CHUNK_MAX_RETRIES=5

# Pausa pequena entre chunks para não sobrecarregar
CHUNK_INTERVAL_MS=200

# -----------------------------------------------------------------------------
# NOTIFICAÇÕES POR EMAIL (CRÍTICAS PARA BANCOS GRANDES)
# -----------------------------------------------------------------------------

# SEMPRE ativar notificações para monitorar backups longos
ENABLE_EMAIL_NOTIFICATIONS=true

# Configurações do email
EMAIL_FROM=backup@suaempresa.com
EMAIL_TO=admin@suaempresa.com,dba@suaempresa.com
EMAIL_SUBJECT_PREFIX=[BACKUP-LARGE-DB]

# Configurações do servidor SMTP
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=backup@suaempresa.com
SMTP_PASSWORD=sua_senha_smtp

# Usar TLS/SSL
SMTP_USE_TLS=true

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES DE WEBHOOK (OPCIONAL)
# -----------------------------------------------------------------------------

# URL do webhook para notificações em tempo real
WEBHOOK_URL=https://hooks.slack.com/seu-webhook

# Nome do webhook
WEBHOOK_USERNAME="Backup Sistema Grande"

# =============================================================================
# ESTIMATIVAS DE TEMPO PARA BANCOS GRANDES
# =============================================================================
# 
# Com essas configurações, para um banco de 200GB com tabela de 80GB:
# 
# 🕐 Tempo estimado total: 8-12 horas
# 📊 Chunks gerados: ~400-500 (tabela 80GB)
# 💾 Espaço em disco necessário: ~60GB (com compressão)
# 🔄 Restauração: 4-6 horas
# 
# 💡 DICAS IMPORTANTES:
# - Execute durante madrugada para menor impacto
# - Monitore logs em /logs/backup.log
# - Verifique espaço em disco antes do backup
# - Teste a restauração em ambiente não-produção
# - Configure alertas para falhas de backup
#
# =============================================================================
