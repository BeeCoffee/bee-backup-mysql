# =============================================================================
# CONFIGURAÇÕES DO SISTEMA DE BACKUP MARIADB/MYSQL - EXEMPLO
# =============================================================================
# Copie este arquivo para .env e configure suas variáveis

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES DOS SERVIDORES DE BANCO DE DADOS
# -----------------------------------------------------------------------------

# Servidor de origem (onde estão os dados para backup)
SOURCE_HOST=localhost
SOURCE_PORT=3306

# Servidor de destino (onde os backups serão restaurados)
DEST_HOST=localhost
DEST_PORT=3306

# Credenciais de acesso aos bancos de dados
DB_USERNAME=backup_user
DB_PASSWORD=ALTERE_ESTA_SENHA

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES DE BACKUP
# -----------------------------------------------------------------------------

# Databases a serem incluídos no backup (separados por vírgula)
DATABASES=database1,database2,database3

# Agendamento do backup (formato cron)
# 0 2 * * * = Todo dia às 02:00
BACKUP_TIME=0 2 * * *

# Número de dias para manter os backups (limpeza automática)
RETENTION_DAYS=7

# Ativar compressão dos backups (true/false)
BACKUP_COMPRESSION=true

# Prefixo dos arquivos de backup
BACKUP_PREFIX=backup

# Opções adicionais do mysqldump
MYSQLDUMP_OPTIONS=--routines --triggers --single-transaction --add-drop-database --default-character-set=utf8mb4

# Executar backup na inicialização do container (true/false)
RUN_ON_START=false

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES DE SISTEMA
# -----------------------------------------------------------------------------

# Timezone do sistema
TZ=America/Sao_Paulo

# Nível de log (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES DE NOTIFICAÇÕES POR EMAIL (OPCIONAL)
# -----------------------------------------------------------------------------

# Ativar notificações por email (true/false)
ENABLE_EMAIL_NOTIFICATIONS=false

# Configurações do email
EMAIL_FROM=backup@exemplo.com
EMAIL_TO=admin@exemplo.com
EMAIL_SUBJECT_PREFIX=[BACKUP-MYSQL]

# Configurações do servidor SMTP
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=backup@exemplo.com
SMTP_PASSWORD=senha_smtp

# Usar TLS/SSL (true/false)
SMTP_USE_TLS=true

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES DE WEBHOOK (OPCIONAL)
# -----------------------------------------------------------------------------

# URL do webhook para notificações (Slack, Discord, Teams, etc.)
# Deixe vazio para desabilitar
WEBHOOK_URL=

# Nome do webhook (aparece nas notificações)
WEBHOOK_USERNAME=Backup System

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES AVANÇADAS (OPCIONAL)
# -----------------------------------------------------------------------------

# Timeout para conexões de banco de dados (segundos)
DB_TIMEOUT=30

# Número máximo de tentativas de conexão
MAX_RETRY_ATTEMPTS=3

# Intervalo entre tentativas (segundos)
RETRY_INTERVAL=5

# Ativar logs detalhados (true/false)
ENABLE_DEBUG_LOGS=false

# Verificar integridade dos backups após criação (true/false)
VERIFY_BACKUP_INTEGRITY=true
