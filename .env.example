# =============================================================================
# CONFIGURAÇÕES DO SISTEMA DE BACKUP MARIADB/MYSQL - EXEMPLO
# =============================================================================
# Copie este arquivo para .env e configure suas variáveis

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES DOS SERVIDORES DE BANCO DE DADOS
# -----------------------------------------------------------------------------

# Servidor de origem (onde estão os dados para backup)
SOURCE_HOST=localhost
SOURCE_PORT=3306

# Servidor de destino (onde os backups serão restaurados)
DEST_HOST=localhost
DEST_PORT=3306

# Credenciais de acesso aos bancos de dados
DB_USERNAME=backup_user
DB_PASSWORD=ALTERE_ESTA_SENHA

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES DE BACKUP
# -----------------------------------------------------------------------------

# Databases a serem incluídos no backup (separados por vírgula)
DATABASES=database1,database2,database3

# Agendamento do backup (formato cron)
# 0 2 * * * = Todo dia às 02:00
BACKUP_TIME=0 2 * * *

# Número de dias para manter os backups (limpeza automática)
# Para DBs grandes, considere 14+ dias devido ao tempo de geração
RETENTION_DAYS=7

# Ativar compressão dos backups (true/false)
BACKUP_COMPRESSION=true

# Prefixo dos arquivos de backup
BACKUP_PREFIX=backup

# Opções adicionais do mysqldump
# Padrão: --routines --triggers --single-transaction --add-drop-database --default-character-set=utf8mb4
# Para DBs grandes, adicione: --quick --lock-tables=false --set-gtid-purged=OFF --column-statistics=0 --disable-keys --extended-insert=false
MYSQLDUMP_OPTIONS=--routines --triggers --single-transaction --add-drop-database --default-character-set=utf8mb4

# Executar backup na inicialização do container (true/false)
RUN_ON_START=false

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES DE SISTEMA
# -----------------------------------------------------------------------------

# Timezone do sistema
TZ=America/Sao_Paulo

# Nível de log (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES DE NOTIFICAÇÕES POR EMAIL (OPCIONAL)
# -----------------------------------------------------------------------------

# Ativar notificações por email (true/false)
ENABLE_EMAIL_NOTIFICATIONS=false

# Configurações do email
EMAIL_FROM=backup@exemplo.com
EMAIL_TO=admin@exemplo.com
EMAIL_SUBJECT_PREFIX=[BACKUP-MYSQL]

# Configurações do servidor SMTP
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=backup@exemplo.com
SMTP_PASSWORD=senha_smtp

# Usar TLS/SSL (true/false)
SMTP_USE_TLS=true

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES DE WEBHOOK (OPCIONAL)
# -----------------------------------------------------------------------------

# URL do webhook para notificações (Slack, Discord, Teams, etc.)
# Deixe vazio para desabilitar
WEBHOOK_URL=

# Nome do webhook (aparece nas notificações)
WEBHOOK_USERNAME=Backup System

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES AVANÇADAS (OPCIONAL)
# -----------------------------------------------------------------------------

# Timeout para conexões de banco de dados (segundos)
# Para DBs grandes, use 300 (5 minutos) ou mais
DB_TIMEOUT=30

# Timeout específico para operações de leitura da rede MySQL (segundos)
# Para DBs grandes (>50GB), recomendado: 7200 (2 horas)
NET_READ_TIMEOUT=600

# Timeout específico para operações de escrita da rede MySQL (segundos)
# Para DBs grandes (>50GB), recomendado: 7200 (2 horas)
NET_WRITE_TIMEOUT=600

# Tamanho máximo de pacote MySQL
# Para DBs grandes, use 1G ou 512M
MAX_ALLOWED_PACKET=128M

# Timeout geral para o processo mysqldump (segundos)
# Para DBs pequenos: 3600 (1h) | Para DBs grandes: 21600-28800 (6-8h)
MYSQLDUMP_TIMEOUT=3600

# Número máximo de tentativas de conexão
# Para DBs grandes, recomendado: 3-5 tentativas
MAX_RETRY_ATTEMPTS=3

# Intervalo entre tentativas (segundos)
# Para DBs grandes, use 10-60 segundos
RETRY_INTERVAL=5

# Ativar logs detalhados (true/false)
# Recomendado: true para DBs grandes para monitoramento
ENABLE_DEBUG_LOGS=false

# Verificar integridade dos backups após criação (true/false)
VERIFY_BACKUP_INTEGRITY=true

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES ESPECÍFICAS PARA DATABASES GRANDES (>50GB)
# -----------------------------------------------------------------------------
# ⚠️  IMPORTANTE: Para databases >50GB, aplique as configurações abaixo

# Ativar modo otimizado para databases grandes
# Valores: auto (detecção automática) | true | false
LARGE_DB_MODE=auto

# Usar extended insert para melhor performance
# Para DBs grandes, recomendado: false (melhor recuperação de erros)
USE_EXTENDED_INSERT=true

# Desabilitar estatísticas de colunas (MySQL 8.0+)
# Para DBs grandes, recomendado: true
DISABLE_COLUMN_STATISTICS=false

# Usar quick mode para economizar memória
# Para DBs grandes, recomendado: true
USE_QUICK_MODE=false

# -----------------------------------------------------------------------------
# EXEMPLO DE CONFIGURAÇÃO PARA DATABASE DE 200GB+
# -----------------------------------------------------------------------------
# Descomente e ajuste para databases muito grandes:

# DB_TIMEOUT=300
# NET_READ_TIMEOUT=7200
# NET_WRITE_TIMEOUT=7200
# MAX_ALLOWED_PACKET=1G
# MYSQLDUMP_TIMEOUT=28800
# MAX_RETRY_ATTEMPTS=5
# RETRY_INTERVAL=60
# ENABLE_DEBUG_LOGS=true
# LARGE_DB_MODE=true
# USE_EXTENDED_INSERT=false
# DISABLE_COLUMN_STATISTICS=true
# USE_QUICK_MODE=true
# RETENTION_DAYS=14
# BACKUP_TIME=0 22 * * *

# -----------------------------------------------------------------------------
# GUIA DE CONFIGURAÇÃO POR TAMANHO DE DATABASE
# -----------------------------------------------------------------------------
# 
# 📦 DATABASES PEQUENOS (<1GB):
# - Use configurações padrão
# - MYSQLDUMP_TIMEOUT=1800 (30 min)
# - DB_TIMEOUT=30
# 
# 📦 DATABASES MÉDIOS (1GB-50GB):
# - DB_TIMEOUT=300 (5 min)
# - MYSQLDUMP_TIMEOUT=7200 (2 horas)
# - NET_READ_TIMEOUT=3600, NET_WRITE_TIMEOUT=3600
# 
# 📦 DATABASES GRANDES (50GB-200GB):
# - DB_TIMEOUT=300, NET_READ_TIMEOUT=7200, NET_WRITE_TIMEOUT=7200
# - MYSQLDUMP_TIMEOUT=21600 (6 horas)
# - MAX_RETRY_ATTEMPTS=5, RETRY_INTERVAL=60
# - USE_QUICK_MODE=true, USE_EXTENDED_INSERT=false
# 
# 📦 DATABASES MUITO GRANDES (>200GB):
# - DB_TIMEOUT=300, NET_READ_TIMEOUT=7200, NET_WRITE_TIMEOUT=7200
# - MYSQLDUMP_TIMEOUT=28800 (8 horas)
# - MAX_RETRY_ATTEMPTS=5, RETRY_INTERVAL=60
# - ENABLE_DEBUG_LOGS=true, RETENTION_DAYS=14
# - Executar em horário de baixo uso: BACKUP_TIME=0 22 * * *
# - Considere dividir em múltiplos databases menores se possível
# 
# 🚨 TROUBLESHOOTING:
# - Erro "Lost connection": Aumente NET_READ_TIMEOUT e NET_WRITE_TIMEOUT
# - Timeout geral: Aumente MYSQLDUMP_TIMEOUT
# - Problemas de memória: USE_QUICK_MODE=true, USE_EXTENDED_INSERT=false
# - Locks na tabela: Adicione --lock-tables=false ao MYSQLDUMP_OPTIONS
