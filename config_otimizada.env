# =============================================================================
# CONFIGURAÇÃO OTIMIZADA PARA TABELA log_instituicao (80GB)
# =============================================================================
# Copie essas configurações para seu .env para otimizar o backup

# 🚀 CONFIGURAÇÕES OTIMIZADAS PARA log_instituicao
CHUNK_SIZE=100000                    # 100k registros por chunk (mais eficiente)
CHUNK_SIZE_THRESHOLD_MB=500          # Detectar tabelas >500MB
CHUNK_TIMEOUT=2700                   # 45min por chunk (mais tempo)
CHUNK_MAX_RETRIES=5                  # Mais tentativas por chunk
ENABLE_AUTO_CHUNKING=true

# ⚡ CONFIGURAÇÕES DE PERFORMANCE
DB_TIMEOUT=300                       # 5min para conexão
NET_READ_TIMEOUT=7200               # 2h para leitura
NET_WRITE_TIMEOUT=7200              # 2h para escrita
MAX_ALLOWED_PACKET=1G               # Pacotes grandes
MYSQLDUMP_TIMEOUT=172800            # 48h total (muito generoso)
ENABLE_DEBUG_LOGS=true              # Logs detalhados

# 📊 RESULTADOS ESPERADOS COM ESSAS CONFIGURAÇÕES:
# - Chunks: ~233 (ao invés de 466)
# - Tempo por chunk: 45min (ao invés de 30min)
# - Tempo total estimado: ~175h (~7 dias)
# - Muito mais eficiente que a configuração anterior

# 💡 DICA: Para testar primeiro, use:
# CHUNK_SIZE=10000    # Chunks pequenos para teste
# CHUNK_TIMEOUT=600   # 10min por chunk para teste
